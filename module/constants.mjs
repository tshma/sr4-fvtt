const ATTRIBUTE_TYPES = ["String", "Number", "Boolean", "Formula", "Resource"];
const types = {
    Runner: 'runner',
    NPC: 'npc',
    Critter: 'critter'
};

export default {
    ATTRIBUTE_TYPES,
    Types: types
};
